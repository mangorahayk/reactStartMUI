import { Button } from '@mui/material';
import React from 'react';

function Home(props) {
    return (
        <div>
           <Button variant='contained'>BBAAREV</Button>
        </div>
    );
}

export default Home;